---
- name: Install Docker on all nodes
  hosts: nodes
  become: true
  vars:
    docker_users:
      - ubuntu
  roles:
    - geerlingguy.docker

  tasks:
    - name: Install Python Docker SDK
      apt:
        name: python3-docker
        state: present
        update_cache: true